import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Hybrid-renewable-energy-generation-solution',
};

export default nextConfig;
